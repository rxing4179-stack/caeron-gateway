import os, re; p=os.path.expanduser('~/caeron-gateway/injection.py'); c=open(p).read(); c=c.replace("rollup_rows = await cursor.fetchall()", "rollup_rows = list(reversed(await cursor.fetchall()))"); c=c.replace("rows = await cursor.fetchall()\n            tag_counts['monthly']", "rows = list(reversed(await cursor.fetchall()))\n            tag_counts['monthly']"); open(p,'w').write(c)